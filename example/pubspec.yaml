name: example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  llm_api_picker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint:

flutter:
  uses-material-design: true
